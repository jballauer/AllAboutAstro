{/* Temporary — remove once the site migration is complete. See GitHub issue tracker. */}
<div class="construction-stamp" role="status">
  <span class="stamp-icon" aria-hidden="true">🔭</span>
  <span class="stamp-text">
    <span class="stamp-title">Under Construction</span>
    <span class="stamp-sub">Moving to a new home</span>
  </span>
</div>

<style>
  @keyframes stamp-fly-in {
    from {
      transform: translateX(260px) rotate(9deg);
      opacity: 0;
    }
    to {
      transform: translateX(0) rotate(9deg);
      opacity: 1;
    }
  }

  .construction-stamp {
    position: absolute;
    top: 14px;
    right: -8px;
    z-index: 50;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    transform: rotate(9deg);
    background: #b3261e;
    color: #fff;
    padding: 0.5rem 1.1rem;
    border: 2px dashed rgba(255, 255, 255, 0.75);
    border-radius: 4px;
    box-shadow: 0 4px 14px rgba(0, 0, 0, 0.6);
    font-family: Arial, Helvetica, sans-serif;
    pointer-events: none;
    animation: stamp-fly-in 0.7s cubic-bezier(0.16, 1, 0.3, 1) both;
  }

  @media (prefers-reduced-motion: reduce) {
    .construction-stamp {
      animation: none;
    }
  }
  .stamp-icon {
    font-size: 1.4rem;
    line-height: 1;
  }
  .stamp-text {
    display: flex;
    flex-direction: column;
    line-height: 1.2;
  }
  .stamp-title {
    font-weight: 700;
    font-size: 0.8rem;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    white-space: nowrap;
  }
  .stamp-sub {
    font-size: 0.65rem;
    font-weight: 400;
    opacity: 0.9;
    white-space: nowrap;
  }

  @keyframes stamp-fly-in-mobile {
    from {
      transform: translateX(260px) rotate(9deg) scale(0.8);
      opacity: 0;
    }
    to {
      transform: translateX(0) rotate(9deg) scale(0.8);
      opacity: 1;
    }
  }

  @media (max-width: 600px) {
    .construction-stamp {
      top: 6px;
      right: -4px;
      transform: rotate(9deg) scale(0.8);
      animation-name: stamp-fly-in-mobile;
    }
  }
</style>
